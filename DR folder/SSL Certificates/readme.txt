To manage the ssl ceritificates follow the folling path using the amin console.
The cretificates were  installed at the cell level.

key store:
Security>SSL certificate and key management > SSL confiturations > elavon > Key store and certificates > CellDefaultKeyStore>Signer certificates

trust store:
Security>SSL certificate and key management > SSL confiturations > elavon > Key store and certificates > CellDefaultTrustStore > Signer certificates
